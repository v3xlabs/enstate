{
	"name": "template-worker-rust",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --port 3001"
	},
	"devDependencies": {
		"wrangler": "^3.19.0"
	}
}

{
	"name": "template-worker-rust",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --local --port 8080"
	},
	"devDependencies": {
		"wrangler": "^3.14.0"
	}
}

pub async fn discover_name(name: String) -> Result<String, ()> {
    // let response = reqwest::get(format!("https://api.github.com/users/{}", name)).await?;
    // let body = response.text().await?;
    // Ok(body)
    todo!()
}
